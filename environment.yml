name: blog-writer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - google-genai
      - google-api-python-client
      - google-auth-oauthlib
      - google-auth-httplib2
      - python-dotenv
      - apscheduler
      - requests
      - beautifulsoup4
      - feedparser
      - markdown
      - python-telegram-bot>=21.0
      - lxml
      - pytrends
